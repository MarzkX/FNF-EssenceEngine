<?xml version="1.0" encoding="utf-8"?>
<project>
	<!-- _________________________ Application Settings _________________________ -->

	<app title="Friday Night Funkin': Essence Engine" file="Funkin" packageName="com.essence.engine" package="com.essence.engine" main="Main" version="0.1.1" company="Mr7kX" />

	<!--Switch Export with Unique ApplicationID and Icon-->
	<set name="APP_ID" value="0x0100f6c013bbc000" />

	<!-- NOTE TO SELF: DISABLE THIS IF ISSUES ARE FOUND -->
	<haxedef name="LINC_LUA_RELATIVE_DYNAMIC_LIB"/> <!-- stable luas PUT AFTER FIRST LINE WITH APP NAME AND ETC -->

	<!--The flixel preloader is not accurate in Chrome. You can use it regularly if you embed the swf into a html file
		or you can set the actual size of your file manually at "FlxPreloaderBase-onUpdate-bytesTotal"-->
	<!-- <app preloader="Preloader" resizable="true" /> -->
	<app preloader="flixel.system.FlxPreloader" />

	<!--Minimum without FLX_NO_GAMEPAD: 11.8, without FLX_NO_NATIVE_CURSOR: 11.2-->
	<set name="SWF_VERSION" value="11.8" />
	
	<!-- ______________________ PSYCH ENGINE CUSTOMIZATION ______________________ -->

	<define name="MODS_ALLOWED" if="desktop" />
	<define name="ESSENCE_WATERMARKS"/>
	<!--define name="GAMEJOLT_ALLOWED" if="desktop" / <- you're in a source code... ok-->
	<define name="PROTOTYPE" if="desktop" />
	<define name="HSCRIPT_ALLOWED" if="desktop" />
	<define name="LUA_ALLOWED" if="desktop" />
	<define name="ACHIEVEMENTS_ALLOWED" />
	<define name="DISCORD_ALLOWED" if="cpp"/>
	<define name="VIDEOS_ALLOWED" if="windows || linux || android" unless="32bits"/> <!-- IF YOU WANT TO USE VIDEOS ON YOUR SOURCE MOD, GET THIS LINE OUTSIDE OF THE SECTION -->

	<!-- ____________________________ Window Settings ___________________________ -->

	<!--These window settings apply to all targets-->
	<window width="1280" height="720" fps="" background="#000000" hardware="true" vsync="false" />

	<!--HTML5-specific-->
	<window if="html5" resizable="true" />

	<!--Desktop-specific-->
	<window if="desktop" orientation="landscape" fullscreen="false" resizable="true" vsync="false"/>

	<!--Mobile-specific-->
	<window if="mobile" orientation="landscape" fullscreen="true" width="0" height="0" resizable="false"/>

	<!--Switch-specific-->
	<window if="switch" orientation="landscape" fullscreen="true" width="0" height="0" resizable="true" />

	<!-- _____________________________ Path Settings ____________________________ -->

	<set name="BUILD_DIR" value="export/debug" if="debug" />
	<set name="BUILD_DIR" value="export/release" unless="debug" />
	<set name="BUILD_DIR" value="export/32bit" if="32bits" />

	<classpath name="src" />

	<!-- __________________________ Game Assets & Etc. __________________________ -->
	
	<!-- Turn on Crash Handler -->
	<define name="CRASH_HANDLER" if="desktop release" />

	<!-- Assets -->
	<assets path="bin/fonts"            embed='false'/>

	<!--section if="windows"-->
		<!--assets path="bin/FlixelCrashHandler.exe" rename="bin/withbugs/FlixelCrashHandler.exe" embed='false' if="CRASH_HANDLER_WITHBUGS"/-->
		<!--assets path="bin/crashassets/" rename='bin/withbugs/assets' embed='false' if="CRASH_HANDLER_WITHBUGS"/-->
		<!--assets path="bin/FlixelCrashHandler.exe" rename="FlixelCrashHandler.exe" embed='false' unless="CRASH_HANDLER_WITHBUGS"/-->
		<!--assets path="bin/crashassets/" rename='bin/crashlogs/assets' embed='false' unless="CRASH_HANDLER_WITHBUGS" -->
	<!--/section-->

	<assets path="funkin/mainFiles/"    rename='funkin'          exclude="*.ogg" if="web"/>
	<assets path="funkin/mainFiles/"    rename='funkin'          exclude="*.mp3" unless="web"/>
	<assets path="funkin/videos"        library="videos"        if="VIDEOS_ALLOWED"/>
	<assets path="funkin/songs"         library="songs"         exclude="*.ogg" if="web"/>
	<assets path="funkin/songs"         library="songs"         exclude="*.mp3" unless="web"/>

	<section if="HSCRIPT_ALLOWED">
	    <assets path="funkin/ingame/scripts" rename='funkin/data/scriptData'/>
	</section>

	<section if="windows">
	    <assets path="funkin/ingame"  library="ingame"  exclude="*.ogg" embed='true' if="web"/>
		<assets path="funkin/ingame"  library="ingame"  exclude="*.mp3" embed='true' unless="web"/>
	</section>
	
	<section unless="windows">
	    <assets path="funkin/ingame"  library="ingame"  exclude="*.ogg" embed='false' if="web"/>
		<assets path="funkin/ingame"  library="ingame"  exclude="*.mp3" embed='false' unless="web"/>
	</section>

	<section if="GAMEJOLT_ALLOWED">
	    <assets path="funkin/gamejolt"  library="gamejolt"      embed='true'    if="windows"/>
		<assets path="funkin/gamejolt"  library="gamejolt"      embed='false'   if='linux'/>
	</section>

	<library name="videos"        preload="true" if="VIDEOS_ALLOWED"/>
	<library name="songs"         preload="true" />
	<library name="week_assets"   preload="true" />

	<section if="MODS_ALLOWED">
		<assets path='bin/example_addons' rename='addons' embed='false'/>
	</section>
	<assets path='bin/compileData/readme.txt' rename='bin/do NOT readme.txt' />

	<!-- _______________________________ Libraries ______________________________ -->

	<haxelib name="flixel" />
	<haxelib name="flixel-ui" />
	<haxelib name="flixel-addons" />
	<haxelib name="tjson" />
	<haxelib name="systools" />
	<haxelib name="tentools" />

	<!--Funkin needed-->
	<haxelib name="haxeui-core" /> <!-- UI framework -->
	<haxelib name="haxeui-flixel" /> <!-- Integrate HaxeUI with Flixel -->
	<haxelib name="thx.semver" /> <!-- Version string handling -->
	<haxelib name="funkin.vis"/>
	
	<!--Psych stuff needed-->
	<haxelib name="linc_luajit" if="LUA_ALLOWED"/>
	<haxelib name="SScript" if="HSCRIPT_ALLOWED"/>
	<haxelib name="hxCodec" if="VIDEOS_ALLOWED"/>
	<haxelib name="discord_rpc" if="DISCORD_ALLOWED"/>
	<haxelib name="flxanimate"/>

	<!-- Disable Discord IO Thread -->
	<haxedef name="DISCORD_DISABLE_IO_THREAD" if="discord_rpc" />
	<haxedef name="NO_PRECOMPILED_HEADERS" if="linux" />

	<!-- Enables a terminal log prompt on debug builds -->
	<haxelib name="hxcpp_debug_server" if="debug"/>
	<haxedef name="HXC_LIBVLC_LOGGING" if="VIDEOS_ALLOWED debug" />

	<ndll name="systools" haxelib="systools" />
	
	<!-- ______________________________ Haxedefines _____________________________ -->

	<!--Enable the Flixel core recording system-->
	<!--<haxedef name="FLX_RECORD" />-->

	<!--Disable the right and middle mouse buttons-->
	<!-- <haxedef name="FLX_NO_MOUSE_ADVANCED" /> -->

	<!--Disable the native cursor API on Flash-->
	<!--<haxedef name="FLX_NO_NATIVE_CURSOR" />-->

	<!--Optimise inputs, be careful you will get null errors if you don't use conditionals in your game-->
	<!-- <haxedef name="FLX_NO_MOUSE" if="mobile" /> -->
	<!-- <haxedef name="FLX_NO_KEYBOARD" if="mobile" /> -->
	<!-- <haxedef name="FLX_NO_TOUCH" if="desktop" /> -->
	<!--<haxedef name="FLX_NO_GAMEPAD" />-->

	<!--Disable the Flixel core sound tray-->
	<!--<haxedef name="FLX_NO_SOUND_TRAY" />-->

	<!--Disable the Flixel sound management code-->
	<!--<haxedef name="FLX_NO_SOUND_SYSTEM" />-->

	<!--Disable the Flixel core focus lost screen-->
	<haxedef name="FLX_NO_FOCUS_LOST_SCREEN" />
	
	<!-- Show debug traces for hxCodec -->
	<haxedef name="HXC_DEBUG_TRACE" if="debug" />
	
	<!--Disable the Flixel core debugger. Automatically gets set whenever you compile in release mode!-->
	<haxedef name="FLX_NO_DEBUG" unless="debug" />

	<haxedef name="CAN_OPEN_LINKS" unless="switch" />

	<!--Enable this for Nape release builds for a serious peformance improvement-->
	<haxedef name="NAPE_RELEASE_BUILD" unless="debug" />
	
	<!--Used for Izzy Engine's crash handler-->
	<haxedef name="HXCPP_CHECK_POINTER" if="CRASH_HANDLER" />
	<haxedef name="HXCPP_STACK_LINE" if="CRASH_HANDLER" />
	<haxedef name="HXCPP_STACK_TRACE" if="CRASH_HANDLER" />
	
	<!--Disable deprecated warnings-->
	<haxedef name='no-deprecation-warnings' />

	<!--Macro fixes-->
	<haxeflag name="--macro" value="allowPackage('flash')" />
	<haxeflag name="--macro" value="include('my.pack')" />
	<haxeflag name="-dce no" />
	<!-- Ensure all Funkin' classes are available at runtime. -->
	<haxeflag name="--macro" value="include('funkin')" />
	<!-- Ensure all UI components are available at runtime. -->
	<haxeflag name="--macro" value="include('haxe.ui.backend.flixel.components')" />
	<haxeflag name="--macro" value="include('haxe.ui.containers.dialogs')" />
	<haxeflag name="--macro" value="include('haxe.ui.containers.menus')" />
	<haxeflag name="--macro" value="include('haxe.ui.containers.properties')" />
	<haxeflag name="--macro" value="include('haxe.ui.core')" />
	<haxeflag name="--macro" value="include('haxe.ui.components')" />
	<haxeflag name="--macro" value="include('haxe.ui.containers')" />

	<!-- _________________________________ Custom _______________________________ -->

	<!--Place custom nodes like icons here (higher priority to override the HaxeFlixel icon)-->

	<assets path='bin/compileData/iconOG.png' rename='icon.png' if="linux" />
	
	<icon path="bin/compileData/icon16.png" size='16'/>
	<icon path="bin/compileData/icon32.png" size='32'/>
	<icon path="bin/compileData/icon64.png" size='64'/>
	<icon path="bin/compileData/iconOG.png" />
</project>
